name: Test
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Do Shared Action
      - uses: MikeHingley22/test-action@latest
      - uses: actions/checkout@v4
      - name: Run a one-line script
        run: echo Hello, world!
